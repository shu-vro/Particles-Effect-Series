$(".fireWorks").fireworks({
    sound: true, // sound effect
    opacity: 0.9,
    width: "100%",
    height: "100%",
});

VanillaTilt.init(document.querySelectorAll('.parallax')), {
    max: 25, 
    speed: 400
}